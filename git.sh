#!/bin/bash

git add .
git commit -m 'PMT is Sensitive, outputs total energy to root file, root makes simple histogram'
# git remote add origin https://github.com/talbert48/milliq.git
git remote -v
git push origin master

